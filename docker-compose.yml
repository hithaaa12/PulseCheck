version: '3.9'

services:

  pulsecheck-api:

    build: .

    container_name: pulsecheck-api

    ports:
      - "8000:8000"

  dashboard:

    build: ./dashboard

    container_name: pulsecheck-dashboard

    ports:
      - "3000:80"

    depends_on:
      - pulsecheck-api